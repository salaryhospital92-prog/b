# إعداد Netlify — دون نشر تلقائي

المشروع مجهّز للبناء على Netlify، لكن بوابة الإصدار توقف أي بناء مرتبط بالمستودع افتراضيًا حتى تعتمد إدارة المستشفى النشر.

## الإعدادات الجاهزة

- أمر البناء: `npm run build:netlify`
- مجلد النشر: `.next`
- إصدار Node.js: `22.13.0`
- التشغيل المحلي عبر Netlify: `npm run dev:netlify`
- منع البناء التلقائي: مفعّل ما لم تكن قيمة `NETLIFY_RELEASE_APPROVED` مساوية لـ `true`.

## متغيرات التشغيل المطلوبة على Netlify

تُضاف من لوحة Netlify أو من أداة الأوامر، ولا تُكتب داخل المستودع:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY` — قيمة سرية ومتاحة لوظائف الخادم فقط.
- `SUPABASE_ANON_KEY`
- `DEMO_MODE=true` خلال مرحلة المراجعة.
- `NETLIFY_RELEASE_APPROVED=false` حتى صدور قرار النشر.

رمز Netlify الشخصي يستخدم فقط كمتغير محلي باسم `NETLIFY_AUTH_TOKEN` عند الربط أو النشر اليدوي، ولا يُحفظ في أي ملف.

## عند صدور قرار النشر

1. ربط المشروع بحساب Netlify إن لم يكن مرتبطًا.
2. إضافة متغيرات التشغيل السابقة بصورة آمنة.
3. تغيير `NETLIFY_RELEASE_APPROVED` إلى `true`.
4. تنفيذ معاينة أولًا، ثم نشر الإنتاج بعد الموافقة النهائية.
